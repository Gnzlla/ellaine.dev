"use client";
import React from "react";

export default function TechStack({ id }: { id: string }) {
  return (
    <>
      <section id={id} className="bg-amber-100 w-full h-screen">
        yo
      </section>
    </>
  );
}
